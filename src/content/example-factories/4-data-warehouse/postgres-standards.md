**Core Principles**
- Use constraints, indexes, and partitioning where appropriate.
- Prefer set-based operations; avoid row-by-row in SQL.

**Examples**
- CTEs, window functions; vacuum and analyze.
